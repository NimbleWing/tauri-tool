# https://taskfile.dev

version: '3'

tasks:
  dev:
    desc: Run Desktop development
    cmds:
      - yarn tauri dev
  dev-android:
    desc: Run Android development
    cmds: 
      - yarn tauri android dev